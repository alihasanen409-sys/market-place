import { InfoPage } from "@/components/info-page";

export default function ContactPage() {
  return (
    <InfoPage title="Contact">
      <p>Email support, seller questions, and buyer order help are handled from the marketplace inbox.</p>
      <p>For deployment, connect a free email provider such as Resend or Brevo in a later production pass.</p>
    </InfoPage>
  );
}
